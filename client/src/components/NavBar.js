import IconSpaceCom from '../icons/spacecom.svg';

export default function NavBar() {

    return (
        <div className="NavBar">
            <img src={IconSpaceCom} alt="Same" className="spacecomicon" />
        </div>
    );
}